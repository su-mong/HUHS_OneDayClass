<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huhs.onedayclass">

    <application
        android:allowBackup="true"
        android:icon="@drawable/img0"
        android:label="@string/app_name"
        android:roundIcon="@drawable/img0"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Game5Activity"></activity>
        <activity android:name=".Game4Activity" />
        <activity android:name=".Game3Activity" />
        <activity android:name=".Game2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>